import CalculatorClient from '@/components/CalculatorClient';

export const metadata = {
  title: 'Glass Pool Fencing Calculator Demo - Expert Fence',
};

export default function GlassDemoPage() {
  return <CalculatorClient demoMode />;
}
