import BalustradeClient from '@/components/BalustradeClient';

export const metadata = {
  title: 'Glass Balustrade Calculator Demo - Expert Fence',
};

export default function BalustradeDemoPage() {
  return <BalustradeClient demoMode />;
}
